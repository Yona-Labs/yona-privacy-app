# Solana RPC URL
SOLANA_RPC_URL=http://127.0.0.1:8899

# ZKCash Program ID
PROGRAM_ID=6Uok9UsjztPC9VJ3a8ZpawzKmgrD2VvMKQGb64FYjhnx

# Server port
PORT=3000

# Merkle tree height (default: 26)
MERKLE_TREE_HEIGHT=26

# Relayer configuration (optional)
# Set RELAYER_ENABLED=true to enable relayer functionality
RELAYER_ENABLED=false

# Relayer wallet private key (JSON format, e.g., [1,2,3,...])
# Get this from: solana-keygen export ~/.config/solana/relayer.json
RELAYER_PRIVATE_KEY=[]

# Address Lookup Table (optional, improves transaction efficiency)
ALT_ADDRESS=

# Fee configuration (only used when relayer is enabled)
MIN_FEE_LAMPORTS=10
MAX_COMPUTE_UNITS=1000000

DATABASE_URL=postgres://postgres:postgres@localhost:5432/default